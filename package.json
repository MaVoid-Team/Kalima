{
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "PORT=5000 npm --prefix kalima-platform/backend run dev",
    "dev:frontend": "npm --prefix kalima-platform/frontend run dev -- --host 0.0.0.0"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.3"
  }
}
